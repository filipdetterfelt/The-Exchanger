public class JCombobox {
    /*
    String[] colorThemes = {"Välj färgtema", "Gul","Rött","Grönt","Blått","Rosa","Grå"};
        colorThemeComboBox = new JComboBox<>(colorThemes);
        colorThemeComboBox.addActionListener(new ActionListener() {
            @Override
            public void actionPerformed(ActionEvent e) {
                String selectedTheme = (String) colorThemeComboBox.getSelectedItem();
                colorThemeComboBox.setForeground(new Color(0x00FF00));
                colorThemeComboBox.setBackground(Color.black);
                //Uppdatera klockans färgtema baserat på användarens val
                if(selectedTheme.equals("Gul")){
                    changeClockTheme(Color.YELLOW);
                }
                else if(selectedTheme.equals("Rött")){
                    changeClockTheme(Color.RED);
                }
                else if(selectedTheme.equals("Grönt")){
                    changeClockTheme(Color.GREEN);
                }
                else if (selectedTheme.equals("Blått")){
                    changeClockTheme(Color.BLUE);
                }
                else if (selectedTheme.equals("Rosa")){
                    changeClockTheme(Color.PINK);
                }
                else if (selectedTheme.equals("Grå")){
                    changeClockTheme(Color.GRAY);
                }

                else{
                    //Om användaren väljer välj färgtema eller inget val görs
                    changeClockTheme(null);
                }

            }
        });

        //Lägg till rullgardinsmenyn för färgtema i din beftinliga layout
        JPanel topPanel = new JPanel(new FlowLayout());
        topPanel.add(colorThemeComboBox);
        this.add(topPanel,BorderLayout.NORTH);

        colorThemeComboBox.setForeground(new Color(0x00FF00));
        colorThemeComboBox.setBackground(Color.black);
        topPanel.setBackground(Color.black);
     */
}
